import ComingSoon from "@/components/common/ComingSoon";

export default function OfflineSOS() {
  return (
    <ComingSoon
      title="Offline SOS"
      description="When the network drops, an automatic SMS goes out to your circle with your last known coordinates, a map link, and a timestamp."
      icon="cellular"
    />
  );
}
